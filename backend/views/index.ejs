<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <meta name="description" content="<%= description %>">
  <meta name="keywords" content="<%= keywords %>">
  <link rel="canonical" href="<%= canonical %>">
  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <style>
    .btn-primary-contrast {
      background-color: #0056b3; /* Darker blue for better contrast */
      color: #ffffff; /* White text */
    }
    .btn-primary-contrast:hover {
      background-color: #004494; /* Slightly darker on hover */
    }
  </style>
</head>
<body>
  <%- include('partials/navbar') %>
  <div class="container mt-5">
    <%- include('calculator') %>    
  </div>
  <div class="container mt-5">
    <% if (partial === 'sip-calculator') { %>
      <%- include('calculators/sip_calculator') %>
    <% } else if (partial === 'simple-sip-calculator') { %>
      <%- include('calculator/simple_sip_calculator') %>
    <% } else if (partial === 'stepup-sip-calculator') { %>
      <%- include('calculators/step_up_sip_calculator') %>
    <% } else { %>
      <h1>Welcome to StepUp SIP Calculator</h1>
      <p>Get detailed information about SIP, mutual funds, and the market. Our website provides tools and resources to help you make informed investment decisions.</p>
      <p>Explore our SIP calculators to plan your investments and achieve your financial goals.</p>
    <% } %>
  </div>
  <%- include('partials/footer') %>
  <!-- Bootstrap JS and dependencies -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
